{
    "name": "my-project",
    "version": "1.0.0",
    "scripts": {
        "build": "cd front2 && npm install && npm run build",
        "serve": "netlify dev"
    },
    "dependencies": {
        "telegraf": "^4.16.3"
    },
    "devDependencies": {
        "netlify-cli": "^6.14.21"
    }
}
